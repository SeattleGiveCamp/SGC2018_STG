Open a bash terminal
navigate to the folder
In the terminal, type "source venv/Scripts/Activate"
Run ngrok with pip(See below)
install stuf
Type "python RESTful_API_prototype"

If python is not you path, open either a python terminal or IDLE
type 'import sys'
type 'print(sys.executable)'
Add the path to your system or user path.

Installing requirements
Run "python -m pip install Flask Flask-Cors Flask-Jsonpify Flask-RESTful Flask-SQLAlchemy Jinja2 SQLAlchemy Click jsonify certifi idna pytz idna six"